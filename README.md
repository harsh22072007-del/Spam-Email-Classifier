# Spam-Email-Classifier
A machine learning–based spam email classifier built using Python and scikit-learn. The project uses TF-IDF for text vectorization and a Naive Bayes model to classify emails as spam or ham, demonstrating core NLP preprocessing, model training, evaluation, and prediction workflows.
